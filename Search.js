import React, { Button } from 'react-native'
import { View,Text } from 'react-native'
function Search() {
  return (
    <View><Button title='back' onPress={() => {handleTabChange2(1)}}></Button></View>
  )
}

export default Search